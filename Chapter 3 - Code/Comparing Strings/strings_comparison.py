# A program to demonstrate how strings can be compared
namel = input('Enter the first name: ')
name2 = input('Enter the second name: ')
if namel == name2:
    print('The names are the same. ')
else:
    print ('The names are NOT the same. ')
